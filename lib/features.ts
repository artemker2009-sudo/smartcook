// Клиентские фиче-флаги. Меняют ТОЛЬКО видимость — никакого похода в БД.
//
// FEATURE_RESTAURANT_GAME: игровой слой «Мой ресторан» (куки, энергия, уровни
// ресторана, награды, лидерборд, бэйджи). Выключен на этапе 4.2 «меньше, но
// лучше». Код и таблицы НЕ удалены — просто ничего игрового не рендерится и не
// синхронизируется. Чтобы вернуть игру — поставить true.
export const FEATURE_RESTAURANT_GAME = false;

// SHOW_COOKED_PHOTO_BLOCK: блок «Приготовили? Покажите результат!» на экране
// рецепта и остальные входы в публикацию фото своего блюда (кнопка на финале
// режима готовки, «Поделиться» в ленте сообщества). Включить, когда появится
// активная аудитория — решение основателя. Код и API не удалены: флаг в true
// возвращает всё как было. Витрина «Приготовили сегодня» на Главной от флага
// не зависит — она показывает уже существующие фото.
export const SHOW_COOKED_PHOTO_BLOCK = false;

// FEATURE_COMMUNITY_FEED: «Лента сообщества» (/feed) целиком. Выключена — в
// проде до неё не добраться никак: пунктов в профиле нет, /feed отдаёт 404,
// API ленты (/api/feed/like, /api/feed/submit, жалоба с postId) — тоже 404.
// Данные, таблицы, админская модерация и удаление постов вместе с аккаунтом НЕ
// тронуты. Витрина «Приготовили сегодня» на Главной и жалобы на её фото
// ({ photoId }) от флага не зависят. Чтобы вернуть ленту — поставить true.
export const FEATURE_COMMUNITY_FEED = false;

// FEATURE_BANQUETS: раздел «Банкеты» целиком. Выключен — в проде до него не
// добраться: пункта в профиле и карточки в «О проекте» нет, /parties и
// /party/* отдают 404, API банкетов (/api/party/*) — 404, server actions
// банкетов отвечают отказом, перенос гостевых банкетов в аккаунт при входе не
// запускается, /parties убран из sitemap. Данные и таблицы НЕ тронуты, удаление
// банкета из админки (/api/admin/parties) работает. Чтобы вернуть банкеты —
// поставить true.
export const FEATURE_BANQUETS = false;

// FEATURE_SHOPPING_SYNC: зеркалирование ЛИЧНЫХ списков покупок на сервер для
// залогиненных (таблица user_shopping_lists, lib/shoppingSync.ts). Выключено:
// ни одного сетевого запроса не уходит, раздел «Покупки» работает ровно как
// раньше — только localStorage устройства. Общие (семейные) списки от флага не
// зависят, это отдельная сущность. Включать ПОСЛЕ прогона миграции
// supabase_user_shopping_lists.sql и приёмки по чек-листу PR.
export const FEATURE_SHOPPING_SYNC = true;

// FEATURE_IDEAS: раздел «Идеи» — каталог рецептов, который наполняем мы сами
// (черновик → вычитка → публикация, как «Кухонные заметки»). Выключен: /ideas
// отдаёт 404, пятой вкладки в таб-баре нет, Главная и остальные разделы не
// меняются вовсе. Включать по этапам — сначала на превью, в прод только после
// приёмки раздела с реальными рецептами.
//
// Включён в прод 22.09.2026 (опубликовано 100 рецептов): пятая вкладка и
// /ideas для всех. Выключить обратно — поставить false, данные не трогаются.
export const FEATURE_IDEAS = true;

// IDEAS_INDEXABLE: отдавать ли «Идеи» поисковикам. Держим false, пока в
// каталоге меньше ~50 опубликованных рецептов: полупустая рубрика в индексе
// работает против SEO, а SEO у нас канал №1.
//
// ОДИН переключатель на обе половины вопроса, чтобы открыть раздел поиску
// можно было одной правкой и ничего не забыть:
//   1) robots: noindex на /ideas и /ideas/<slug> (app/ideas/**/page.tsx);
//   2) /ideas и каждый опубликованный /ideas/<slug> в app/sitemap.ts.
// Ставим true — и раздел открывается целиком. Действует только вместе с
// FEATURE_IDEAS: при выключенном разделе адреса отдают 404, в карте их нет.
//
// Открыт 22.09.2026: опубликованных 100, порог в 50 пройден.
export const IDEAS_INDEXABLE = true;

// SHOW_WELCOME: приветственная плашка нового посетителя («Привет! SmartCook —
// твой ИИ-шеф», components/modals/OnboardingModal.tsx). Выключена 24.09.2026:
// её закрывают почти все, пользы нет. Компонент и ключ localStorage
// smartcook_onboarding_seen НЕ тронуты — вернуть можно этой одной строкой.
//
// Побочный эффект, который учтён в коде: плашка установки на Главной (iOS)
// ждала ключа знакомства, чтобы не наслаиваться на него
// (components/InstallBanner.tsx). Ключ теперь не ставит никто, поэтому при
// выключенном флаге «не первый визит» проверяется по следам прошлых визитов
// (hasReturningTrace) — иначе эта плашка не показалась бы никогда.
export const SHOW_WELCOME = false;

// FEATURE_PREMIUM: платный Премиум — недельный лимит подборов, шторка «подборы
// закончились», оплата через Robokassa, блок «Премиум» в профиле и разделы
// «Платежи»/«Пользователи» в админке.
//
// ЧИТАЕТСЯ ИЗ ОКРУЖЕНИЯ, а не задано константой, как остальные флаги. Причина
// одна: включать Премиум нужно СНАЧАЛА только на превью, не выкатывая его в
// прод, а константа в коде одинакова везде, куда доехал коммит. Переменная
// задаётся в Vercel для окружения Preview; в Production её нет → флаг false →
// в проде ничего не меняется. Включит прод основатель сам, добавив переменную.
//
// NEXT_PUBLIC_, потому что флаг нужен и клиенту (шторка, кнопки, счётчик), и
// серверу (проверка лимита в роутах). Значение подставляется на сборке.
//
// ЧТО ОТ ФЛАГА НЕ ЗАВИСИТ И РАБОТАЕТ ВСЕГДА:
//   * страницы /premium и /oferta — их открывает и проверяет Robokassa;
//   * запись в podbor_usage — данные копим с первого дня, иначе включать лимит
//     будет вслепую (SPEC 3.2).
export const FEATURE_PREMIUM = process.env.NEXT_PUBLIC_FEATURE_PREMIUM === "true";
